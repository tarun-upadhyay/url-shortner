import Image from 'next/image'
import Header from './Components/Header'
import { getToken } from "next-auth/jwt"
import handler from './api/auth/[...nextauth]/options'
export default function Home() {
  
  return (
   <div>lorem*50</div>
  )
}
